import { Container, Typography } from "@mui/material";
// import { useNavigate } from "react-router-dom";

const Home = () => {
  // const navigate = useNavigate();

  return (
    <Container disableGutters maxWidth="lg">
      <Typography>HOLA SDASDASD</Typography>
    </Container>
  );
};

export default Home;
